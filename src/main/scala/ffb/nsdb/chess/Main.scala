package ffb.nsdb.chess

import ffb.nsdb.chess.board.{GameStateService, GameStateServiceImpl, CastleRights, PieceBoard}

object Main extends App {

  // object bs extends GameStateServiceImpl
  // import bs._

  // object ps extends PieceServiceImpl
  // import ps._

  // val initial = "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"
  // val next = "rnbqkbnr/pppppppp/8/8/4P3/8/PPPP1PPP/RNBQKBNR b KQkq e3 0 1"
  // val third = "rnbqkbnr/pp1ppppp/8/2p5/4P3/8/PPPP1PPP/RNBQKBNR w KQkq c6 0 2"

  // val one = bs.setBoardState(initial)
  // val two = bs.setBoardState(next)
  // val thr = bs.setBoardState(third)

  // println(bs.toString(one))
  // println(bs.toString(two))
  // println(bs.toString(thr))

  // // println(bs.getPieceAtSquare(thr, "c", 5))
  // // println(bs.getPieceAtSquare(thr, "d", 5))
  // // println(bs.getPieceAtSquare(thr, "e", 5))
  // // println(bs.getPieceAtSquare(thr, "f", 5))
  // // println(bs.getPieceAtSquare(thr, "g", 5))
  // // println(bs.getPieceAtSquare(thr, "h", 5))
  // // for (i <- 0 to 63) yield {
  // //   println(thr.squares(i).value)
  // // }
}
